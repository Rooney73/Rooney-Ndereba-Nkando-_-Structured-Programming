#include <stdio.h>
#include <string.h>

int main() {
    char name[50];


    printf("Enter your name: ");
    scanf("%s", name);

    printf("You entered: %s\n", name);
    printf("Length of the string: %lu\n", strlen(name));

    return 0;
}
